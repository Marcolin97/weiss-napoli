// Shared domain constants for client-side use.
// Values must stay in sync with POINTS_PER_WIN and DEFAULT_ROUND_COUNT in types/domain.ts.

/** Valid round counts for a tournament: exactly 3 or 4 (domain rule). */
export const DEFAULT_ROUND_COUNT = 3 as const
